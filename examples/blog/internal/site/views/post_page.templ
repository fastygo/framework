package views

import "github.com/fastygo/blocks/editorial"

templ PostPage(data PostPageData) {
	@editorial.PostArticle(editorial.PostArticleProps{
		HTMLContent: data.HTMLContent,
		BackHref:    "/",
		BackLabel:   "← Back to all posts",
	})
}
